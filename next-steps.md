# Next Research Steps

- What changes need to be made to match paper 4's exact zitterbewegung? Also, if the non-trivial QCA lives on a BCC lattice instead of 3d, what are the smallest changes needed to enact that?, also, if we do require enforcing $n^2 + m^2 = 1$ in the Dirac coupling what changes need to be made?
- attempt to falsify the F3 lensing prediction failure at low fermion density.
- Review the current model for any nonsensical constructs or assumptions and detail them in the changelog.
- run fresh tests on all model elements with a factor of 10 variation to all variables (e.g. if we were running 100 steps, run 1000) determine if any of the new data matches any existing formula or data point (e.g. the larger the number of steps, the closer we get to an approximation of an imaginary number).
- investigate where the 1/√6 offset in the $O(k^2)$ equation is coming from. hypothesize how it could possibly be originating in the geometric factor from the four-neighbor tetrahedral structure of the lattice and how that could be tested (e.g. change the lattice structure to 3-neigbhor and the offset should follow).